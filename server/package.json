{
  "name": "digifunzi-landing-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone JSON-backed API for the Digifunzi landing site — /api/public/* content + form submission storage.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
