node_modules
dist
dist-ssr
Guide/*.zip
*.local

# Standalone API runtime data — form submissions. Content JSON under
# data/content/ IS tracked; the submission stores are not.
data/leads.json
data/contacts.json
data/*.tmp
data/**/*.tmp

# Environment files — all kept out of git. Recreate from .env.example
# (see README.md for the real deploy values).
.env
.env.*
!.env.example
!.env.server.example
!server/.env.example

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Puppeteer / build artifacts
.cache
